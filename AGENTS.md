# Agent Instructions
# 月嫂 AI 业务助手 - Codex 项目规则

## 项目目标

本项目是一套面向月嫂公司和母婴服务机构的 B 端业务软件。

所有开发必须优先考虑：

1. 真实业务可用性
2. 数据一致性
3. 页面操作效率
4. PC 端 SaaS 体验
5. 手机端实际可操作性
6. UI 完成度
7. 后续业务流程可调整性

---

## 前端任务规则

任何涉及：

- 页面开发
- UI修改
- 布局修改
- 响应式
- 交互
- 表格
- Card
- Modal
- Drawer
- Timeline
- Tabs
- 表单

的任务：

开始前必须先读取：

DESIGN.md

并遵守其中的设计规范。

如果已安装相关 Skill：

优先使用：

frontend-design
ui-ux-pro-max
Product Design
frontend-testing-debugging / Playwright

但只加载当前任务真正需要的 Skill。

不要为了一个小任务同时调用大量重叠 Skill。

---

## UI 修改流程

所有重要前端修改必须按照：

分析现有页面

↓

明确页面真实用户任务

↓

检查 DESIGN.md

↓

修改

↓

启动真实页面

↓

浏览器检查

↓

桌面截图

↓

手机截图

↓

检查交互

↓

检查 Console

↓

必要时继续修改

↓

最后再汇报

不得：

只修改代码
+
build 成功

就宣布 UI 完成。

---

## 浏览器验证

如果 Browser / Playwright 可用：

所有重要 UI 修改完成后必须实际打开页面验证。

最低检查：

PC：
1440 × 900

Mobile：
390 × 844

重点检查：

页面是否正常渲染

是否有横向滚动

导航是否正常

按钮是否可点击

Modal / Drawer 是否正常

固定导航是否遮挡内容

文字是否截断

Console 是否出现相关 error

---

## UI 审美要求

禁止默认生成典型 AI Admin Dashboard。

禁止：

- 紫蓝渐变
- 玻璃拟态
- 满屏大 Card
- 过量 Shadow
- 过量 Icon
- 巨大留白
- 全页面同一种布局
- 为高级感强行增加动画

必须根据业务决定页面形式。

---

## 数据规则

客户
月嫂
档期
匹配
跟进
服务记录
媒体

必须尽量使用统一 Source of Truth。

禁止：

客户页一套数据

月嫂页另一套数据

档期页第三套数据。

任何业务状态变化：

应该同步影响相关页面。

---

## 业务规则

当前 Demo 中的：

锁档
上户
下户
服务完成
客户状态

属于需求验证阶段假设。

禁止把行业流程硬编码散落到大量组件。

状态枚举和业务规则应集中管理。

以后真实公司提出不同流程时，应容易调整。

---

## AI 匹配规则

AI 不得直接决定一切。

匹配逻辑优先：

硬条件过滤
+
软条件评分
+
AI解释

AI负责：

自然语言理解
需求整理
推荐解释
话术生成

确定性代码负责：

档期
冲突
硬条件
状态
核心业务约束

---

## 图片规则

月嫂图片统一通过 Media 数据模型管理。

不要在多个组件分别维护独立照片数组。

Demo：

Binary 图片使用 IndexedDB。

Metadata：

进入统一 Demo store。

禁止：

大量 Base64 写 localStorage。

正式版未来使用对象存储。

---

## 修改原则

优先：

小而完整的修改。

避免为了新需求：

大规模重构现有稳定功能。

修改现有 V3 功能时：

必须检查是否造成回归。

---

## 完成任务前

至少执行：

项目 build / typecheck

如果前端页面发生变化：

执行浏览器视觉 QA。

如果业务流程发生变化：

至少手动走一遍对应核心路径。

---

## 汇报要求

完成开发后简洁报告：

1. 改了什么
2. 哪些业务功能联动
3. 哪些页面验证过
4. Build 是否通过
5. 浏览器测试是否通过
6. 是否还有已知问题

不要输出大段无关代码解释。
