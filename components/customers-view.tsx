'use client';import { Filter,Plus,Search,UsersRound } from 'lucide-react';import { useMemo,useState } from 'react';import type { Customer,CustomerStatus } from '@/lib/types';import { customerStatuses } from '@/lib/status';import { StatusBadge } from './status-badge';import { Button } from './ui/button';import { Input } from './ui/input';import { Table,TableBody,TableCell,TableHead,TableHeader,TableRow } from './ui/table';
const nextLabel=(c:Customer)=>c.status==='新客户'?'整理需求':c.status==='待匹配'?'智能匹配':c.status==='已推荐'?'查看推荐':c.status==='沟通中'?'安排面试':c.status==='已锁定'?'查看档期':c.status==='服务中'?'新增跟进':'查看记录';
export function CustomersView({customers,onOpen,onCreate,onMatch}:{customers:Customer[];onOpen:(id:string)=>void;onCreate:()=>void;onMatch:(id:string)=>void}){const [query,setQuery]=useState('');const [filter,setFilter]=useState<'全部'|CustomerStatus>('全部');const [owner,setOwner]=useState('全部负责人');const visible=useMemo(()=>customers.filter(c=>(`${c.name}${c.phone}${c.city}`.includes(query.trim()))&&(filter==='全部'||c.status===filter)&&(owner==='全部负责人'||c.consultant===owner)),[customers,query,filter,owner]);return <div className="page-stack"><div className="page-heading"><div><div className="eyebrow">客户 Pipeline</div><h1>客户</h1><p>管理客户需求、匹配进度和跟进状态。</p></div><Button className="primary-button" onClick={onCreate}><Plus/>新建客户</Button></div><section className="pipeline-surface"><div className="pipeline-toolbar"><div className="search-box"><Search/><Input value={query} onChange={e=>setQuery(e.target.value)} placeholder="搜索姓名、电话或地区"/></div><select value={filter} onChange={e=>setFilter(e.target.value as typeof filter)}><option>全部</option>{customerStatuses.map(x=><option key={x}>{x}</option>)}</select><select value={owner} onChange={e=>setOwner(e.target.value)}><option>全部负责人</option><option>王敏</option><option>周晴</option><option>赵宁</option></select><Button variant="outline"><Filter/>更多筛选</Button><span>{visible.length} 位客户</span></div>{visible.length?<><div className="desktop-table crm-table"><Table><TableHeader><TableRow><TableHead>客户</TableHead><TableHead>联系电话</TableHead><TableHead>预产期 / 服务</TableHead><TableHead>地区</TableHead><TableHead>预算</TableHead><TableHead>当前阶段</TableHead><TableHead>推荐</TableHead><TableHead>负责人</TableHead><TableHead>最近跟进</TableHead><TableHead>下一步</TableHead></TableRow></TableHeader><TableBody>{visible.map(c=><TableRow key={c.id}><TableCell><button className="customer-person" onClick={()=>onOpen(c.id)}><span className="customer-list-avatar">{c.name[0]}</span><span className="link-name">{c.name}</span></button></TableCell><TableCell>{c.phone}</TableCell><TableCell>{c.dueDate.slice(5).replace('-','月')}日 · {c.serviceDays}天</TableCell><TableCell>{c.city}</TableCell><TableCell>¥{c.budgetMin.toLocaleString()}–{c.budgetMax.toLocaleString()}</TableCell><TableCell><StatusBadge status={c.status}/></TableCell><TableCell>{c.recommendedCount?`${c.recommendedCount}位`:'—'}</TableCell><TableCell>{c.consultant}</TableCell><TableCell>{c.lastFollowUp}</TableCell><TableCell><button className="next-action" onClick={()=>c.status==='待匹配'?onMatch(c.id):onOpen(c.id)}>{nextLabel(c)}</button></TableCell></TableRow>)}</TableBody></Table></div><div className="mobile-customer-list">{visible.map(c=><button className="customer-card" key={c.id} onClick={()=>onOpen(c.id)}><div><span className="mobile-customer-person"><span className="customer-list-avatar">{c.name[0]}</span><strong>{c.name}<small>{c.city}</small></strong></span><StatusBadge status={c.status}/></div><p>{c.dueDate} · ¥{c.budgetMin.toLocaleString()}–{c.budgetMax.toLocaleString()}</p><footer><span>{c.lastFollowUp}</span><b>{nextLabel(c)}</b></footer></button>)}</div></>:<div className="empty-state"><UsersRound/><h3>没有找到“{query}”相关客户</h3><Button variant="outline" onClick={()=>{setQuery('');setFilter('全部')}}>清除筛选</Button></div>}</section></div>}
