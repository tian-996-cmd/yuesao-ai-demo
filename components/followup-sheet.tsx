'use client';import { useState } from 'react';import { Button } from './ui/button';import { Textarea } from './ui/textarea';import { Sheet,SheetContent,SheetDescription,SheetFooter,SheetHeader,SheetTitle } from './ui/sheet';
export function FollowupSheet({open,onOpenChange,onSave}:{open:boolean;onOpenChange:(x:boolean)=>void;onSave:(text:string)=>void}){const [text,setText]=useState('已电话联系客户，确认候选月嫂资料和下一步面试时间。');return <Sheet open={open} onOpenChange={onOpenChange}><SheetContent className="compact-sheet"><SheetHeader><SheetTitle>新增跟进</SheetTitle><SheetDescription>保存后客户时间线和老板工作台会立即同步。</SheetDescription></SheetHeader><div className="sheet-body"><Textarea rows={7} value={text} onChange={e=>setText(e.target.value)}/></div><SheetFooter><Button variant="outline" onClick={()=>onOpenChange(false)}>取消</Button><Button className="primary-button" onClick={()=>{if(text.trim()){onSave(text.trim());onOpenChange(false)}}}>保存跟进</Button></SheetFooter></SheetContent></Sheet>}
